/**
 * Marketing Layout
 *
 * Simple layout wrapper for the Mission Connect Studio.
 * The page handles its own header and footer.
 */
export default function MarketingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
