/**
 * Auth Configuration Placeholder
 *
 * This app uses Mission Connect's existing authentication.
 * No additional auth is required.
 */

export const auth = null;
export const signIn = null;
export const signOut = null;
